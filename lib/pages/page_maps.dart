import 'package:flutter/material.dart';
class page_maps extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Maps Page'),
    );
  }
}