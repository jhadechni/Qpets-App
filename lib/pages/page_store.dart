import 'package:flutter/material.dart';
class page_store extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Store Page'),
    );
  }
}